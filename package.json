{
  "name": "clone-tabnews",
  "version": "1.0.0",
  "description": "A project created during the classes of curso.dev",
  "main": "index.js",
  "scripts": {
    "postgres": "psql --host=localhost --username=postgress --port=5432",
    "docker-detach": "docker compose --file infra/compose.yaml up --detach",
    "nex-dev": "next dev",
    "dev": "npm run nex-dev & npm run docker-detach & npm run postgres",
    "lint:check": "prettier --check .",
    "lint:fix": "prettier --write .",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "next": "^13.1.6",
    "pg": "^8.11.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "jest": "^29.6.2",
    "prettier": "^3.5.2"
  }
}
